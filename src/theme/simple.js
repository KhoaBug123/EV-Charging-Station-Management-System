// Simple SkaEV Theme for testing
import { createTheme } from "@mui/material/styles";

const skaevTheme = createTheme({
  palette: {
    primary: {
      main: "#1379FF",
    },
    secondary: {
      main: "#B5FF3D",
    },
  },
  typography: {
    fontFamily: '"Inter", sans-serif',
  },
});

export default skaevTheme;
